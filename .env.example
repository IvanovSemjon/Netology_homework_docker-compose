# Security: Generate strong secret key
SECRET_KEY=your-very-long-secret-key-here-change-this-in-production
DEBUG=0
ALLOWED_HOSTS=localhost,127.0.0.1,nginx

# Database configuration
DATABASE_URL=postgresql://stocks_user:secure_password@postgres:5432/stocks_db

# PostgreSQL specific (alternative to DATABASE_URL)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=stocks_db
DB_USER=stocks_user
DB_PASSWORD=secure_password_change_in_production
DB_HOST=postgres
DB_PORT=5432